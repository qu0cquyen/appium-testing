name: Flutter CI
on: 
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
jobs:
  build_apk:
    name: Build Flutter (Android)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v1
        with:
          java-version: "12.x"
      - uses: subosito/flutter-action@v1
        with:
          chanel: "stable"
      - name: Get Dependencies
        run: flutter pub get
      - name: Build APK
        run: flutter build apk --debug >> ./build/apk/app.apk
      - uses: actions/upload-artifact@v3
        with:
          name: release-apk
          path: output/apk/app-release.apk
  
